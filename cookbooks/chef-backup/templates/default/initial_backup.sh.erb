#!/bin/bash
<%node[:backup][:jobs].each do |name, config|%>
if [ ! -d "/home/<%=node[:deploy_user][:user]%>/Backup/data/<%=name%>" ]; then
    # Control will enter here if $DIRECTORY exists
    <%=node[:languages][:ruby][:bin_dir]%>/backup perform -t <%=name%>
fi
<%end%>
