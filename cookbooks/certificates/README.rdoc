= DESCRIPTION:
Default recipe sets up all machines to use certificate authority
generated by 
rake ca_cert

The ssl_cert definition setups up machines to install ssl certificates
generated by 
rake signed_ssl_cert FQDN=<common name for ssl certificate (eg.
secure.example.com)>
= REQUIREMENTS:

= ATTRIBUTES:

= USAGE:

